apiVersion: v1
kind: Service
metadata:
  name: tududi
  labels:
    app.kubernetes.io/name: tududi
spec:
  selector:
    app.kubernetes.io/name: tududi
  ports:
    - name: http
      port: 3002
      targetPort: http
