import type { ElementDetectedType } from '../@types/tagify';
interface SpanElement {
    text: string;
    color: string;
    style?: any;
    onClick?: (text: string, type: ElementDetectedType) => void;
}
export declare const SpanElement: (props: SpanElement) => JSX.Element;
export {};
