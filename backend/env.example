# tududi Production Environment Configuration
# Copy this file to .env and modify the values as needed

# =============================================================================
# REQUIRED ENVIRONMENT VARIABLES
# =============================================================================

# Node.js environment (production, development, test)
NODE_ENV=production

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Database file path (relative to backend directory)
DB_FILE=db/production.sqlite3

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Host to bind to (0.0.0.0 for all interfaces)
HOST=0.0.0.0

# Port to listen on
PORT=3002

# =============================================================================
# SECURITY & AUTHENTICATION
# =============================================================================

# Session secret for cookies (generate with: openssl rand -hex 64)
TUDUDI_SESSION_SECRET=your-super-secret-session-key-here

# Default user email (will be created if doesn't exist)
TUDUDI_USER_EMAIL=admin@yourdomain.com

# Default user password (will be created if doesn't exist)
TUDUDI_USER_PASSWORD=your-secure-password-here

# =============================================================================
# FRONTEND & CORS CONFIGURATION
# =============================================================================

# Frontend URL (used for redirects and CORS)
FRONTEND_URL=http://localhost:8080

# Allowed origins for CORS (comma-separated list)
# Include your frontend domain(s) here
TUDUDI_ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3002,http://127.0.0.1:8080,http://127.0.0.1:3002

# =============================================================================
# FILE UPLOADS
# =============================================================================

# Path for file uploads (relative to backend directory)
TUDUDI_UPLOAD_PATH=uploads

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Disable task scheduler (true/false)
DISABLE_SCHEDULER=false

# Disable Telegram integration (true/false)
DISABLE_TELEGRAM=false

# =============================================================================
# GOOGLE CALENDAR INTEGRATION (OPTIONAL)
# =============================================================================

# Google OAuth client ID for calendar integration
# GOOGLE_CLIENT_ID=your-google-client-id

# Google OAuth client secret for calendar integration
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Google OAuth redirect URI (defaults to http://localhost:3002/api/calendar/oauth/callback)
# GOOGLE_REDIRECT_URI=http://yourdomain.com/api/calendar/oauth/callback

# =============================================================================
# PRODUCTION DEPLOYMENT NOTES
# =============================================================================

# 1. Copy this file to .env: cp env.example .env
# 2. Modify the values according to your environment
# 3. Ensure TUDUDI_SESSION_SECRET is a strong, random value
# 4. Set appropriate TUDUDI_ALLOWED_ORIGINS for your domain
# 5. Use strong passwords for TUDUDI_USER_PASSWORD
# 6. Consider using environment-specific .env files for different deployments

# =============================================================================
# SECURITY RECOMMENDATIONS
# =============================================================================

# - Never commit .env files to version control
# - Use different session secrets for each environment
# - Regularly rotate passwords and secrets
# - Use HTTPS in production
# - Restrict TUDUDI_ALLOWED_ORIGINS to only necessary domains
# - Consider using a secrets management service in production
